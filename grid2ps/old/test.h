C This structure converts the integers id(16) & id(17) to characters
        structure /str2real/
           union
              map
		 Real val
              end map
              map
                 Character*10 str
	      end map
           end union
        end structure

	Record /str2real/ value 

	Common value 
